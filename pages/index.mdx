import { BlogLayout } from '../components/blog_layout'

export const meta = {}

My name is George. I write software. Mostly in Go.

I work on [Flipt](https://flipt.io).

Previously, I worked on [InfluxDB](https://influxdata.com), [CodeShip](https://codeship.com), [Honeycomb TV](https://honeycomb.tv) and a bunch more lost to time.

export default ({ children }) => <BlogLayout meta={meta}>{children}</BlogLayout>
